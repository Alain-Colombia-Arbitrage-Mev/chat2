# ==========================================================================
# Public API endpoint — your deployed memory.ancestro.ai backend
# ==========================================================================
VITE_API_URL=https://memory.ancestro.ai

# ==========================================================================
# Default agent id for /widget?agent= (informational only — the backend
# is single-tenant at the moment, but keep this so future multi-agent
# routing can plug in without code changes).
# ==========================================================================
VITE_DEFAULT_AGENT_ID=default
